# Sidepanel 多标签设计

记录 sidepanel 从"单一 pin 一个 hostname"改造成"多标签"的设计决策，供实现时参照，也供以后回顾"为什么这么设计"。这是产品/交互设计文档，不是长期编码规范（那是 [AGENTS.md](./AGENTS.md)）。

## 背景

现状：sidepanel 打开时 pin 住一个 hostname，之后**不跟随**浏览器标签页切换（`entrypoints/sidepanel/use-pinned-hostname.ts`），只在浏览器活跃标签页的 hostname 变了时显示一条 `TabChangedBanner`，用户手动点了才切换。这是为了避免"每瞟一眼别的标签页就打一整轮 Cloudflare API 请求"。

问题：用户需要来回看多个 host（比如同时盯着几个 Worker 的灰度发布），现在的模型逼着他们要么反复手动切 pin（丢失之前的状态），要么开多个浏览器窗口各开一个 sidepanel。

## 术语

- **浏览器标签页**：Chrome 本身的标签页。
- **面板标签**：sidepanel 内部自己渲染的标签栏里的一项，分两种：
  - **固定标签**：用户主动固定、持久化保存的面板标签。
  - **机动标签**：唯一的一个"跟随浏览器标签页"的面板标签，不持久化，内容随浏览器活跃标签页的 hostname 实时变化。

sidepanel 本身还是每个浏览器窗口一个实例（Chrome side panel API 不支持真正同时开多个面板），多标签是在这一个实例内部自己实现的标签栏。

## 数据模型

```ts
interface PinnedTab {
  hostname: string; // 唯一键
  forcedTokenId: string | null; // 这个 host 单独锁定的账号，独立于其他标签
}
```

（自定义命名 `label` 这轮不做 UI，先不加这个字段；顺序就是数组顺序，不需要 `addedAt`。）

- `PinnedTab[]`：有序数组，持久化在 `chrome.storage.local`（不用 `sync`——和现有 Token 存储的约定一致，这类信息偏敏感，不推云端）。
- 机动标签**不持久化**，是纯派生状态：当前浏览器活跃标签页的 hostname，来自现有的 `useLiveTabHostname`。
- 哪个面板标签当前处于焦点（激活态）：**每个 sidepanel 窗口实例各自的运行时状态，不持久化**，重开 sidepanel 从初始规则重新决定（见下）。固定标签列表本身建议**全局共享**（多个 sidepanel 窗口看到同一份），但当前焦点各窗口独立——可以同时在两个窗口盯着不同站点。

## 机动标签状态机

触发源只有一个：`useLiveTabHostname()` 变化（浏览器标签页切换 / 当前标签页内导航到新 host）。每次变化：

1. **当前焦点本来就在机动标签上，且新 host 命中某个固定标签** → 焦点顺势切到那个固定标签（不是"抢"，是同一份内容换成它的canonical 固定实例，避免和固定标签同时展示同一个 host）。
2. **当前焦点本来就在机动标签上，新 host 不在固定标签里，且是 http(s)** → 机动标签内容原地切换成新 host（同一个格子换内容，不是新开标签），标签栏里机动标签的文字跟着变。
3. **新 host 非 http(s)（`chrome://`、新标签页等）或没有活跃标签页** → 机动标签显示"未检测到站点"空态，不影响其他固定标签。
4. **当前焦点在某个固定标签上（不是机动标签）** → 浏览器标签页切换只更新机动标签在标签栏里的显示内容（供用户瞟一眼），**绝不自动抢焦点**——哪怕新 host 命中了另一个固定标签，也不跳过去。这条规则完全取代现有的 `TabChangedBanner`/`isStale` 机制。规则 1 和规则 4 的边界很明确：只有"焦点已经在机动标签上"这一种情况才会发生自动切换，焦点在任何固定标签上时永远不被浏览器标签页切换打断。
5. **单向关系**：点击切换面板标签（固定或机动）**不会**去激活对应的浏览器标签页；只有浏览器标签页驱动机动标签，反过来不成立。

## 固定 / 关闭 / 手动新建

- 机动标签在标签栏里 hover 时在域名文字后面浮出 📌「固定」（见下面"视觉与页面结构"）：点击后把当前显示的 host 写入 `PinnedTab[]`，插入标签栏。固定后机动标签立刻按状态机规则 1 重新求值。
- 每个固定标签 hover 时浮出 ✕「取消固定」：从存储删除。若关掉的正是当前焦点标签，焦点顺移到左边相邻的固定标签；没有相邻的就落回机动标签。
- 「+」手动新建：弹出 host 输入框（可粘贴完整 URL，取 hostname），确认后**直接作为固定标签插入**（没有对应的活跃浏览器标签页背书，天生不可能是机动的，不需要先出现在机动标签再固定这道手续）。新建后立刻切换焦点到这个新标签（见下面挂载策略，这一步天然满足"首次查看即挂载"）。

## 挂载策略（懒挂载 + keep-alive）

目的：固定标签数量增多，不应该导致「打开 sidepanel 变慢、一次性打一堆请求」。

- Sidepanel 内部维护运行时 `mountedTabs: Set<hostname>`（纯 React state，**不持久化**，每次重新打开 sidepanel 从空/仅含初始焦点标签开始）。
- 初始只有本次打开后**第一个被激活显示**的标签在这个集合里——冷启动不存在"恢复上次焦点标签"这回事（焦点本来就不持久化，见"数据模型"一节）：有 popup 交接的 hostname 就用它，没有就是机动标签当前指向的 host。其余固定标签只是标签栏里的一个入口，不渲染内容组件、不发请求。
- 用户第一次点某个固定标签 → 把它的 hostname 加入 `mountedTabs`，此时才真正 mount、真正 fetch。之后切走切回，只是 CSS 隐藏/显示（类似 Radix Tabs 但不 forceMount 卸载的反向操作：mount 过一次之后永远保留），组件树一直在，不二次请求。
- 机动标签被固定时：把它已挂载的组件实例/状态原地转成固定标签（复用 `mountedTabs` 里已有的这个 hostname），不因为"从机动变固定"这个动作本身触发多余的重新请求。
- 机动标签本身**不走这套 keep-alive**：host 变了就是一次全新 mount（会有 loading 闪一下），这是有意为之——"跟随每一次切换代价较高"这个顾虑被精确收窄到只影响机动标签这一个格子，固定标签完全不受影响。

## Favicon：做过，又去掉了

曾经用 Chrome 内置的 `chrome-extension://<id>/_favicon/` 接口给每个标签显示站点图标（需要 `manifest.permissions` 里的 `"favicon"`）。第三轮视觉打磨时为了"更简洁的 Tab List"整个去掉了——标签现在是纯文字，`favicon` 权限、`shared/tabs/favicon-url.ts`、`PanelTabFavicon` 组件都随之删除。

留个记录是因为：如果以后想加回来，这条路是验证过可行的（不用外部服务、无网络请求、找不到图标时 Chrome 会自动兜底一个通用图标、读的是本地缓存所以不要求当前开着该站点的浏览器标签页）。真要加回来，别忘了同时把 `favicon` 权限加回 `wxt.config.ts`。

## 视觉与页面结构（第二轮定稿，取代了最初的"渐进披露 State A/B"设计）

第一版做过"0 个固定标签时大标题、不显示标签栏"的特殊态，用户体验反馈"没有设计感"之后推翻重做，改成下面这套——**标签栏永远渲染，不管有几个固定标签**，不再区分 State A/B：

- **页面头部**：左边是标签栏（`PanelTabStrip`，可横向滚动），右边是固定不滚动的账号控件（`AccountControl`）——挂到头部之后，它显示/操作的是**当前激活 Tab 自己的**账号锁定状态，不是全局唯一账号（账号锁定这个能力仍然是 per-tab 的，只是控件位置统一了）。
- **标签本身极简**：纯文字，没有 favicon、无背景色块、无边框；当前激活的标签用 accent-color（`text-primary`，即 Cloudflare 橙红 `#ea580c`，已经是 `shared/ui/globals.css` 里的 `--primary`，不用新定颜色）；非激活标签中性灰。字体用无衬线大字号 + 压缩字形（`font-stretch-extra-condensed`）+ `tracking-tight`，让域名能在有限宽度里横向压紧显示。固定标签和机动标签用同一个内部组件渲染，样式逻辑完全一致。
- **操作按钮在文字之后，hover 才展开**：静止状态下一个标签就只有域名文字本身。hover（或键盘 focus）到这一条目时，标签**向右延展出宽度**，操作图标同时**从小 scale 上来**——两段动画配合，图标是"长"进新腾出来的空间里，而不是空间一出现就直接显形。固定标签的操作是 ✕（取消固定），机动标签是 📌（固定）。

- **「+」手动新建**始终可见，不再有"只在标签数 > 0 才出现"这道门槛——标签栏本身既然总是渲染，入口复杂度也就不需要再跟标签数量挂钩递进了。
- **其余内容一律下沉到具体 Tab 自己的内容区**，不再有任何"共享头部行"：`RefreshButton`、`DetectionModeToggle`（机动标签专属）都在对应 `PanelTabPane` 内部顶端，`AccountControl` 是唯一真正共享、挂在页面头部的控件。

### 怎么让用户看懂"固定 vs 机动"这两个概念

标签栏样式统一之后带来一个新问题：这两类标签看起来一模一样，用户凭什么知道"有几个是我自己钉的、有一个是跟着浏览器跑的"。定下来的做法基于两条判断：

1. **标记那个唯一的例外，而不是标记多数。** 固定标签会有好几个，机动标签永远只有一个。给每个固定标签挂图钉又贵又啰嗦；给唯一的异类加一点特征，成本低、对比强。所以：固定标签保持纯文字，机动标签带特征。
2. 具体特征 = **分组竖线 + live 点**：
   - 固定标签组和机动标签之间一条极淡的竖线。这借用了 Chrome 自己"固定标签聚在左边"的既有认知，不用重新教。**只在真的有固定标签时才出现**——初始状态只有一个机动标签时不画这条线，没有两组就没有"分组"可言。
   - 机动标签的域名前面一个小圆点，表达"实时/跟随"。圆点用 `bg-current` 直接继承文字当前的颜色状态（激活是橙红、否则是中性灰），不引入第二种颜色。hover 圆点会有 tooltip 说明它的含义，让这个标记自己解释自己。
   - hover 时浮出的 📌 刚好接上下一句话："你可以把它变成固定的"。

还讨论过、但**本轮没做**的一类补充：用行为教学——切换浏览器标签页时让机动标签的文字带过渡动画（放大"只有它在动"这个事实）、点 📌 后让标签从机动位置滑进固定组、机动标签因命中固定标签而消失时做收起动画。这几个都能进一步强化概念，需要时再加。

## 内容区：从"四张卡"改成"一份文档"

标签栏定稿后，下面四块内容（`WorkerStatsCard` / `DeploymentBar` / `RecentErrorsPanel` / `BindingsPanel`）的 shadcn 默认卡片风格就明显落后了。诊断出四个问题，对应四条规则：

1. **边框没承担任何信息。** 四张卡叠起来就是四个矩形框，边框既不是可点击边界也不是分组语义。→ 全部去掉卡片，改用**极淡的分隔线**分节（`version-switcher.tsx` 上的 `divide-y`），配合每节自带的垂直内边距。整个面板从"一摞盒子"变成"一份文档"。
2. **标题有四种写法**（`CardTitle` / `AccordionTrigger` / `font-mono text-[9px]` / 干脆没有）。→ 抽出 `entrypoints/sidepanel/panel-section.tsx`：统一的小标题（沿用标签栏的压缩大写，缩小并转灰，读作大字标签的下一级）+ 可选的标题链接 + 可选的右侧操作。需要把这个标题样式贴到自己元素上的地方（如 Bindings 那个"触发器本身就是标题"的 accordion）用导出的 `PANEL_SECTION_HEADING_CLASS`。
3. **最该一眼扫到的数字反而最小**（`text-sm`，和正文一样大）。→ WorkerStats 三个数字放大到 `text-2xl` 压缩字形，标签用小号灰字放上面、左对齐；错误率 > 0 时转红，正常时中性，让状态自己说话。
4. **唯一有设计感的 DeploymentBar 被稀释了**——那条深色 bar 被包在一张和其他三张一样的普通卡里。→ 去掉外层卡片后，它成为整个面板里唯一的深色块，不用加任何东西就突出了。

另外错误列表原来每条都是"红框红底"，堆四五条非常吵。改成**左侧一条细红竖线 + 正常背景**，红色只留给状态码点缀——信息密度不变，噪音大降。

两个满屏空态（`NoTokenEmptyState`、`ManualDetectionPending`）**保留卡片形态**：它们是替代整个内容区出现的，不会和上面几节并排，卡片在那里仍然合适。

## 流量横条：从"进度条"改成"仪器"

这条 bar 是插件的招牌，用户打开第一眼就该看到它。第一版实现只剩"深色圆角 + 两个渐变色块 + 居中百分比"，是任何 dashboard 都长的样子——demo 里让它独特的东西（横轴同时是 hash 空间、请求落点、历史刻痕）全丢了。诊断和对应的改法：

**基础精修**

1. **绿色是外来的，而且语义是错的。** `emerald-500/700` 不在调色板里（`globals.css` 只有 neutral + 品牌橙 + destructive），而且绿橙对比暗示"好 vs 警告"——但两个 slot 只是"旧版本 vs 新版本"，没有好坏。→ **slot A 中性深灰（incumbent），slot B 品牌橙（正在推进的）**。橙色那块长大 = 灰度在推进，推满整条变橙，颜色本身就在讲故事。`version-slot.tsx` 的文字色同步改（`text-emerald-700` → `text-neutral-700`）。
2. **渐变纯装饰**，不编码任何信息 → 改纯色平涂。
3. **`h-9`（36px）撑不起首屏主角**，上面标签栏是 `text-2xl`，主次反了 → 逐步加高到 `h-16`（64px），条上的百分比同步放大到 `text-2xl`。注意 `TRACK_HEIGHT_PX` 常量要跟着改（波浪几何依赖它），编辑态滑块把手也要跟着高（`h-18`，上下各留 8px overhang）。
4. **百分比藏在色块里，`LABEL_MIN_PERCENT = 14` 以下就消失**——恰恰是最该看清的 canary 比例（1%、5%）看不到。

   中间试过"移到分界线下方做一行 caption"，但百分比本来就该长在横条上；最终解法是**标签逃逸**（见下）。

**百分比标签的"逃逸"机制**

两个版本时，两个百分比都直接画在横条上。难点是窄到几个像素的段（1% 的 canary）放不下文字：

- 段够宽 → 标签在自己段内居中，白字，用和 TabList 里 host 一样的压缩无衬线字形（`font-stretch-extra-condensed` + `tracking-tight`）。
- 放不下 → 标签**越过分界线跑到对面那个宽段上**，但穿上**自己那一侧颜色的小色片 + 白字**，所以仍然看得出它属于谁。
- 之所以能这么做而不出乱子：两个数恒等于 100，阈值取 12%（远小于 50%），**任何时候最多只有一个会放不下**，两个标签永远不会同时逃逸、也就不会撞在一起。
- 逃逸的标签离分界线留 `LABEL_ESCAPE_GAP_PX`（8px），刚好避开 12px 宽的波浪带，也避开编辑态的滑块把手。
- 为什么不直接把橙字写在深灰底上：对比度不够，而且两个同色数字会分不清归属。

**"装得下"是按像素动态判定的，不是固定百分比阈值。** 最初写死 14%，但这个数字同时依赖字号和面板宽度——两者任何一个变了它就悄悄变错。现在的做法：

- 用 `ResizeObserver` 量出轨道实际像素宽度（顺带也让面板缩放时判定继续成立）。
- 标签自身宽度按字符估算（数字 / `%` / `.` 各有一个 em 步进常量），**不是实测**——实测需要"先渲染再量"两趟，而这个值在拖拽时每帧都在变。
- 判定：`段的像素宽度 ≥ 标签估算宽度 + 两侧留白`。
- 居中位置也随之改成按像素钳制（中心离两端至少半个标签 + 留白），取代了原先写死的 8%/92%。

效果：阈值会自己跟着面板宽度走——288px 轨道上约 19% 才装得下，500px 上 9.3% 就够了。全宽度区间（288/320/350/400/500px × 0.5%–99.5%）实测最小余量稳定在 6px（正是设定的留白值），不溢出、不重叠，也不会出现两个标签同时逃逸。

`LABEL_FIT_FALLBACK_PERCENT` 只用于首帧（`ResizeObserver` 还没报宽度时），之后就不再参与判断；取值偏保守，宁可让临界标签"先是色片、再落回段内"，也不要"先溢出、再修正"。

动态判定的实际收益：后来把字号从 `text-xl` 加到 `text-2xl`，**判定逻辑一行没改**，阈值自己从 ~16% 涨到 ~19%（288px 面板）。换成固定阈值的话，这一步改字号就会静默引入溢出。

**`VersionSlot` 里原本还有一份同样的百分比**（也是 `text-2xl`），在百分比搬上横条之后就成了重复——同一个数字、同样大小、上下叠着抢同一次扫视。已删除，`VersionSlot` 现在只负责 tag / version id / 角色徽标 / 钉住按钮 / 预览链接。

**单版本部署也画标签**（整条居中的"100%"）。这里踩过一个坑：最初它是被刻意省略的（单色条上一个孤零零的 100% 显得多余），当时 `VersionSlot` 还印着同一个数字；等到把 `VersionSlot` 的百分比作为"重复"删掉之后，单版本就变成**一处都不显示百分比**了。删掉某处冗余显示前，先确认它不是别的分支唯一的信息来源。

**让它成为特色的两笔**

5. **刻度尺**：上沿 5% 细刻度 + 10% 主刻度，用 `repeating-linear-gradient` 画（不是几十个 DOM 节点）。这是把"进度条"变成"仪器"的关键一笔，直接呼应「横轴是被均匀划分的 hash 空间，分界线落在其中某处」这个原始概念。
6. **历史刻痕**：bar 上方用同样的细刻度画出这次 rollout 前几次部署的分界位置，越旧越淡——一眼看到"这个 Worker 的灰度推进轨迹"。数据来自 `shared/cloudflare-api/deployments.ts` 新增的 `boundaryTrail`：从最新部署往回走，**只要版本集合不变就继续收集**，一旦不同就停（那是另一次 rollout，它的分界位置说明不了当前这次的进展）；slot 顺序也算进 key，因为顺序决定 Durable Object 归属，交换顺序确实是另一回事。单版本部署没有分界，返回空。上限 4 条，再多就从"轨迹"变成"噪点"。拖拽时不显示——那时它会被误读成"你拖过的位置"。

**分界是一条流动的波浪边**：查看态下两块颜色不是被一条直缝分开，而是被一条小幅起伏、整体仍然垂直的波浪边分开，缓慢向下流动（`globals.css` 里的 `flarepeek-boundary-flow`），读作"流量正实时从这一点穿过"。

演进中被推翻过两版，两次都是实现上的偷懒：

> **第一版：明暗呼吸**（4s 的 opacity 循环）——太不明显，几乎看不出来。"流动"比"渐隐"更能表达实时性，而且和原始 demo"请求落下"的隐喻是一路的。
>
> **第二版：直边色块 + 上面盖一条白色波浪描边**——真正的颜色分界仍然是笔直的竖线，波浪只是贴上去的装饰。**正确的做法是让颜色分界本身就是波浪**：波浪左边就是左边的颜色，右边就是右边的颜色。

当前实现的关键点：

- **颜色分界本身就是波浪**：在分界处叠一条 12px 宽的窄带，用**两个填充路径**把这条窄带按波浪重新上色（左侧 `fill-neutral-700`，右侧 `fill-primary`），波浪是两块填充的**公共边**。没有描边。两个 path 放在同一个 `<g>` 里一起做动画，保证公共边始终严丝合缝。
- **不规则但严格可循环**：波形是**三个正弦叠加**（振幅 1.15 / 0.7 / 0.45，相位错开），看起来不像教科书正弦。关键在于每个分量在一个 tile 高度内都走**整数圈**（3 / 5 / 7 圈），所以叠加后的波形在 `WAVE_TILE_PX` 上严格首尾相接——平移一个 tile 后完全对齐，循环无缝。**不能用真随机**：那样既破坏可循环性，也会让波形每次重渲染都变样。
- **两处必须同步的常量**：动画的 `translateY` 距离必须等于 `WAVE_TILE_PX`（当前都是 96px）；改一个就要改另一个，两个文件里都写了交叉提醒。tile 越大，循环周期越长、重复越不易察觉（当前 96px / 14s）。
- 路径从 `-WAVE_TILE_PX` 画到轨道底部，保证平移过程中窄带始终被完全覆盖。
- **两端跳过不画**：分界正好落在轨道边缘时，窄带会有一半被裁掉，剩下一半会把错误的颜色刷到 bar 的端头上——所以 `percentB` 为 0 或 100 时不渲染这条波浪。
- 查看态容器加了 `overflow-hidden` 让窄带干净裁边；这个不能加到编辑态容器上——那里的滑块把手比轨道高，必须允许溢出。

走 `motion-safe:`：用户要求减少动效时波浪静止不动，但**仍然是一条波浪边**，形态本身不依赖动画。编辑态不用波浪，那时分界由滑块把手承担，两个标记叠在同一位置会打架。

## 机动标签的检测模式：自动 / 手动

问题：机动标签默认跟随浏览器标签页——用户随手切换标签页时，也在不停拿新 host 去试探是不是自己的 Cloudflare 资产（`useWorkerLookup` 挨个 Token 尝试）。这本来就是旧版"sidepanel 不跟随标签页"设计要刻意避免的事，这次为了机动标签的跟随体验精确收窄了影响范围，但没有消除——加一个开关，让用户自己决定要不要这份"背景探测"。

- **入口**：机动标签**自己的内容区顶部**（不是共享头部）始终有一个不起眼的小图标（雷达），点开是一个带说明的 Popover + 开关。不设固定标签数量门槛——一直存在，只是视觉上刻意低调，不抢主流程的注意力。
- **自动模式（默认）**：现状不变，浏览器标签页一变机动标签就跟着重新检测。
- **手动模式**：浏览器标签页切换时，机动标签只更新标签栏里显示的 hostname（这部分本来就是免费的，`useLiveTabHostname` 只是查 `chrome.tabs`，不涉及 Cloudflare API），但**不触发**真正的 Cloudflare 检测/请求。机动标签内容区显示一个待检测的空态（hostname + 简短说明 + 一个检测按钮），用户点了才第一次去查。
- **加载入口 = 刷新入口**：不是两套按钮。现有的刷新按钮（`PanelTabPane` 里 `refreshKey` 那套机制）本来就是"要不要重新 fetch"的触发点，手动模式只是把"这个 pane 刚出现时要不要自动触发第一次 fetch"也交给同一个开关——手动模式下默认不自动触发，用户点刷新按钮就等于"检测"。按钮的 tooltip 按状态区分文案：还没查过时是"检测此站点"，查过之后是"刷新"，同一个按钮，同一段逻辑，只是文案跟着状态换。
- **只影响机动标签**：固定标签永远是"首次查看即自动加载"（懒挂载规则本来就保证了它不会无谓触发），这个开关不影响固定标签的行为。

技术上：`shared/worker-panel/use-worker-lookup.ts` 需要加一个 `enabled = true` 参数和一个新的 `idle` 状态——不传这个参数时行为完全不变，popup 那边的调用不受影响。

**文案上需要额外花心思的点**（用户特别提到）：

- 待检测空态不能看起来像"坏了"或"卡住了"，要明确说明"这是手动检测模式，点击按钮开始"。
- 开关本身的 tooltip/说明要交代清楚*为什么*要开手动模式（隐私/性能角度：不想让每个随手逛的网站都被拿去试探是不是 Cloudflare 资产），不能只是一个没有上下文的 on/off。

## 被这次改动取代的现有机制

- `usePinnedHostname` 的单一 pin 概念 → 被 `PinnedTab[]` + 机动标签取代。
- `TabChangedBanner` / `isStale` → 被机动标签状态机规则 4 取代，不再需要额外 banner。
- `sidepanel-handoff.ts`（popup"打开完整面板"时的一次性 hostname 交接）→ 语义不变，交接目标从"设成唯一的 pin"变成"作为机动标签的初始值（如果已经是固定标签，直接跳过去）"。
- `entrypoints/background/tab-badge/tab-badge-orchestrator.ts`（工具栏图标角标）完全独立，不受影响。

## 尚未细化、留给实现阶段的点

- 标签栏溢出（固定标签很多时怎么横向滚动/收纳）——先做最简单版本，不设硬性数量上限。
